# AI 면접 훈련 플랫폼

> 하루 12분의 루틴을 통해 면접 역량을 습관화하는 AI 기반 면접 훈련 서비스

uv run python -c "from app.repository.database import Base, engine; import app.repository.model_registry; Base.metadata.create_all(engine)"